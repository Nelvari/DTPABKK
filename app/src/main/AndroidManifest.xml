<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dtpabkk">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".initial"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.DTPABKK"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".EditUserActivity"></activity>
        <activity android:name=".EditDanaActivity" />
        <activity android:name=".AddDanaActivity" />
        <activity android:name=".AddUserActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".BulanActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".DanaActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>